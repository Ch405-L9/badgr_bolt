# Copy this file to keystore.properties and fill in your values.
# keystore.properties is gitignored — NEVER commit credentials.
#
# Place badgr_release.jks in the project root (same level as app/).
#
storeFile=../badgr_release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=badgr_bolt
keyPassword=YOUR_KEY_PASSWORD
